name: Simple Workflow

on: push

jobs:
  simple-job:
    name: Simple Job
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run a successful command
      run: echo "This is a successful test run!"
